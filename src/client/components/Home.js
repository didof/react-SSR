import React from 'react'

function Home() {
  return (
    <div>
      <div>Home component</div>
      <button onClick={() => alert('Yep')}>Does JS works?</button>
    </div>
  )
}

export default Home
